{
  "_purpose": "Durable session-handover context for the Job Bot project. Update this file when compacting chat history so a fresh session can resume without re-deriving decisions. Keep it factual and current; prune anything stale. Update 'last_updated' and append to 'changelog' on each edit.",
  "schema_version": "1.0",
  "last_updated": "2026-08-04",
  "project": {
    "name": "jobbot",
    "description": "Discord bot that discovers software-engineering internships via a configurable search API (Serper/Bing/Brave/Google PSE), extracts + de-duplicates + relevance-filters postings across major ATS platforms, and posts them to Discord as embeds with action buttons.",
    "repo": "https://github.com/YheChen/Job-Bot",
    "default_branch": "master",
    "language": "Python 3.12+",
    "layout": "src/ layout; package at src/jobbot",
    "local_path": "/Users/yanzhenchen/Desktop/GitHub Projects/Job Bot"
  },
  "deployment": {
    "target": "User's Lenovo ThinkPad T14 Gen 1 home server (Fedora Workstation, dual-boot with Windows; Docker + compose preinstalled, firewalld + SELinux stay ON). Deploy via Docker Compose + Postgres using docker-compose.prod.yml (DEPLOY.md Path A).",
    "alternatives": "Path B: systemd + SQLite bare-metal (deploy/jobbot.service) for Oracle Cloud VM / Raspberry Pi. Path C: Fly/Railway/Render/AWS. All documented in DEPLOY.md.",
    "database": "Postgres in compose for the ThinkPad deployment; SQLite remains the zero-service default for bare-metal (DATABASE_URL switch, no code changes). Only Postgres-specific feature is the cross-process scan advisory lock.",
    "sqlite_path_note": "Bare-metal SQLite needs an ABSOLUTE path, e.g. sqlite+aiosqlite:////opt/jobbot/jobbot.db (four slashes).",
    "prod_compose_notes": "docker-compose.prod.yml is STANDALONE (not an overlay): 127.0.0.1-only port binds (Docker publishing bypasses firewalld), restart: unless-stopped, POSTGRES_PASSWORD is REQUIRED via .env (no default — GitGuardian flags literal creds; also baked into pgdata on first init).",
    "laptop_server_prep": "logind HandleLidSwitch=ignore, mask sleep/suspend/hibernate targets, Linux as GRUB default — commands in DEPLOY.md Path A.",
    "network_gotcha": "Bot only makes OUTBOUND connections; no inbound ports/forwarding/firewall changes on any path. /health binds to localhost."
  },
  "key_decisions": [
    "Deterministic-first: query gen, URL canonicalization, dedup, and relevance scoring are pure/tested functions. LLM classification is opt-in (ENABLE_LLM_CLASSIFICATION) and only refines an already-passing result.",
    "Protocol-based extension points: SearchProvider and PlatformAdapter are drop-in. PlatformRegistry maps hostnames to adapters.",
    "Bounded query generation (no Cartesian explosion): templates per (platform, title-group), decorated with term/location clauses; select_batch reserves top-priority queries and rotates a window over the rest.",
    "Job identity via dedup_key: platform+requisition-id -> canonical URL -> normalized company+title. Re-discovery updates last_seen; job_versions snapshot only on material content change; Discord posting gated on posted_to_discord.",
    "SQLite chosen over Postgres for single-instance simplicity; advisory lock is dialect-aware (real pg lock on Postgres, no-op on SQLite where the scheduler's asyncio lock serializes scans).",
    "Postgres drivers (asyncpg/psycopg) moved to optional [postgres] extra so base install is ARM-friendly with no compiled deps."
  ],
  "current_state": {
    "branch": "master (clean)",
    "tests": "104 passing (pytest). Mostly pure logic; tests/test_scan_listings_integration.py runs the real ScanService against in-memory SQLite + httpx.MockTransport (no network).",
    "db_schema": "15 tables, created via alembic upgrade head (migrations/versions/0001_initial.py uses metadata.create_all)",
    "lint": "ruff clean; ruff format applied across codebase (ruff 0.15.21)",
    "deployment_status": "NOT yet deployed anywhere — the bot has never made a real Discord connection or real search-API call; everything verified via tests/mocks. First real run = deploying to the ThinkPad (DEPLOY.md Path A).",
    "merged_prs": [
      "#1 Run without Docker: SQLite default + Linux VM deployment",
      "#2 Add CI workflow (ruff + pytest + migration smoke) and fix lint",
      "#3 Add pre-commit hooks + enforce formatting in CI",
      "#4 Add handover.json for session/context handover",
      "#5 Add DEPLOY.md + production compose file",
      "#6 Add SimplifyJobs listings source (+ 2 pipeline bug fixes)"
    ]
  },
  "discovery_paths": {
    "search": "SearchProvider (Serper/Bing/Brave/Google PSE) over generated site: queries — consumes API quota.",
    "listing_sources": "jobbot/sources/ — curated feeds mapped straight to ExtractedJob, no page fetch, no search quota. SimplifyJobs listings.json enabled by default (ENABLE_GITHUB_LISTINGS), ETag-conditional so an unchanged 11 MB feed costs a 304. Live check 2026-08-04: 489 candidates in a 30-day window, 319 passing the 0.55 gate.",
    "shared_tail": "Both paths converge on ScanService._ingest (dedup -> score -> persist), then _post_pending (expiration -> poster)."
  },
  "conventions": {
    "ci": ".github/workflows/ci.yml runs on push/PR to master: ruff check, ruff format --check, pytest, and a SQLite alembic upgrade head smoke test.",
    "pre_commit": ".pre-commit-config.yaml (ruff lint+format + hygiene hooks). Enable with 'make hooks' after 'make install'. Pinned: ruff v0.15.21, pre-commit-hooks v6.0.0.",
    "workflow": "Work on a branch, open a PR, wait for CI green, squash-merge, delete branch. Never commit directly to master.",
    "commit_trailer": "Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>",
    "secrets": "Never commit .env. Config is validated by Pydantic (src/jobbot/config.py) and fails fast."
  },
  "quick_reference": {
    "setup": "make install && make hooks",
    "migrate": "make migrate  (alembic upgrade head)",
    "run": "make run  (or: jobbot / python -m jobbot.main)",
    "test": "make test  (pytest -q)",
    "keyless_dry_run": "set SEARCH_PROVIDERS=mock in .env to exercise the pipeline with no API key",
    "required_env": ["DISCORD_TOKEN", "one search provider key (or SEARCH_PROVIDERS=mock)"],
    "health_endpoint": "http://localhost:8080/health (and /ready for DB connectivity)"
  },
  "architecture_pointers": {
    "entrypoint": "src/jobbot/main.py",
    "config": "src/jobbot/config.py",
    "scan_orchestrator": "src/jobbot/services/scan_service.py",
    "scheduler": "src/jobbot/scheduler/runner.py",
    "search_providers": "src/jobbot/search/ (base.py protocol, manager.py fallback)",
    "query_generation": "src/jobbot/queries/ (terms.py vocab, generator.py)",
    "platform_adapters": "src/jobbot/platforms/ (base.py, registry.py, per-ATS)",
    "parsing": "src/jobbot/parsing/ (url canon, ssrf guard, jsonld, fetcher, extractor)",
    "scoring": "src/jobbot/scoring/relevance.py",
    "dedup": "src/jobbot/dedup/detector.py",
    "expiration": "src/jobbot/expiration/checker.py",
    "discord": "src/jobbot/bot/ (client, embeds, views, permissions, cogs/jobs.py)",
    "models": "src/jobbot/db/models.py"
  },
  "feature_flags_and_roadmap": {
    "enabled_now": "search -> extract -> dedup -> filter -> relevance -> expiration -> Discord embeds + buttons; all slash commands; per-guild scheduling.",
    "off_by_default": [
      "LLM classification (ENABLE_LLM_CLASSIFICATION) - Anthropic refinement step",
      "Playwright rendering for JS-only job pages (install [playwright] extra) - relevant for Workday/Oracle/SuccessFactors which are often JS-rendered"
    ],
    "scaffolded_not_wired": [
      "Digests (hourly/daily/weekly) and per-category subscription roles - schema (subscriptions, job_categories) and digest embed builder exist; scheduling them is the next increment"
    ]
  },
  "open_items": [
    "Deploy to the ThinkPad home server via DEPLOY.md Path A (Docker Compose) — this is the first REAL run; watch how extraction/scoring behave on live ATS pages. Consider GITHUB_LISTINGS_LOOKBACK_DAYS=3 for the first scan so the feed backlog does not trickle out 25/scan.",
    "Optional: enable GitHub branch protection on master requiring the 'test' status check (must be toggled in GitHub Settings > Branches; cannot be set via CLI here).",
    "Consider Playwright path for Workday/Oracle/SuccessFactors postings if JSON-LD extraction returns empty over plain HTTP.",
    "Consider a live-API integration test (skipped unless a key env var is set) to catch ATS HTML changes."
  ],
  "changelog": [
    "2026-07-14 Initial handover.json created after PRs #1-#3 (SQLite/no-Docker, CI, pre-commit).",
    "2026-08-04 PR #6: added jobbot/sources/ + SimplifyJobs listings source (ETag-conditional, category/lookback filters, http(s)-only URL guard). Refactored ScanService: extracted shared _ingest() used by both search and feeds; Poster contract now returns {job_id: message_id} of ACTUALLY delivered jobs so the service owns posted-state. Fixed 2 pre-existing bugs the new integration test caught: (1) _touch_existing walked the lazy job.sources relationship -> MissingGreenlet on every duplicate re-discovery; (2) failed Discord sends were marked posted and never retried. Also generalized term parsing (parse_internship_term) which was hardcoded to 2027 and silently dropped 2026/2028 terms.",
    "2026-08-02 PR #5: DEPLOY.md (3 deployment paths + troubleshooting), standalone docker-compose.prod.yml, POSTGRES_PASSWORD in .env.example, README deployment section slimmed to a pointer. Deployment target changed: user bought a ThinkPad T14 (Fedora, dual-boot) as home server, replacing the Oracle VM plan."
  ]
}
